# SEO Fix Progress

## Phase 1 — Critical
- [x] Schema ID standardization — changed `"@id": ".../#organization"` to `"#org"` in **108 files**. Confirmed exactly one canonical Organization `@id` (`#org`) used site-wide; `BaseHead.astro` only contains a historical comment mentioning the old name, not a live usage.
- [x] Missing H1s — root cause found and fixed differently than expected. `journal/*.astro` detail pages don't need H1 added: they already get `<h1>{title}</h1>` automatically from `JournalArticleLayout.astro`. The real bug was `src/pages/insights/{Glossary,AqlTool,FobCalculator,LeadTimeEstimator}.astro` — accidental duplicate copies of files that belong in `src/components/`, left behind in `src/pages/` where Astro was building them as live, broken, head-less orphan routes (`/insights/Glossary`, `/insights/AqlTool`, etc., plus `/insights/SETUP_INSTRUCTIONS` which was **publicly exposing internal dev setup notes**, and `/insights/learning-hub` from a stray `.ts` file). Deleted all 6 stray files. Verified nothing in the codebase links to those paths, and confirmed via build output that those broken routes are gone and the legitimate `src/components/` versions (imported correctly by `insights/index.astro`, which already has its own H1) are untouched.
- [x] Duplicate titles — found via exact-match scan of all 302 built pages' `<title>` and meta description tags (not template-name guessing). Two real cases:
  - **Title dup**: `apparel-manufacturers.astro` and `top-apparel-manufacturers.astro` had the identical title AND near-identical H1/body content (essentially two versions of the same page — see note below). Gave `top-apparel-manufacturers.astro` a distinct title/description reflecting its actual unique sections (OEM vs ODM vs CMT comparison, production spec table, buyer FAQ): new title "Apparel Manufacturers: OEM vs ODM vs CMT Explained | SDF" (56 chars), new description (157 chars). Left `apparel-manufacturers.astro`'s title/description unchanged as the canonical general page.
  - **Meta description dup**: `knitwear-factory.astro` and `knitwear-exporter.astro` shared one generic description despite having different titles and different H1 angles (factory/production capacity vs. export/shipping markets). Wrote two distinct descriptions matching each page's real content (158 / 148 chars).
  - **EXACT FINAL COUNT: 3 pages had their title and/or meta description text changed for uniqueness in this item** (`top-apparel-manufacturers.astro`, `knitwear-factory.astro`, `knitwear-exporter.astro`). Method: extracted `<title>` and `<meta name="description">` from all 302 built HTML pages with a script and grouped by exact string match — not by scanning source filenames/templates — so this count is a precise, build-verified figure.

## Open note for site owner — NOT fixed in this pass (out of metadata-only scope)
`apparel-manufacturers.astro` and `top-apparel-manufacturers.astro` don't just share a title — their H1, intro paragraph, and quick-stats content are near-duplicates of each other (two versions of what reads like the same page, with inconsistent facts: one says "27 years," the other says "Since 1998" — which is 28 years from 2026). I only changed metadata here because Phase 1 is scoped to content/metadata/schema, not layout/content restructuring. Recommend a follow-up decision: merge the two pages, 301-redirect one into the other, or clearly differentiate their actual content (not just metadata) — otherwise Google may still treat them as duplicate/cannibalizing content regardless of the title fix.

## Phase 2 — High priority (not started, see Part 2 task)
## Phase 3 — Medium priority (not started, see Part 3 task)
## Phase 4 — Low priority + architecture (not started, see Part 4 task)

## Notes / Open Questions
- Pre-existing (not introduced by this work) Vite build warning: `Generated an empty chunk: "bangladesh-vs-china-clothing-manufacturing.astro_astro_type_script_index_0_lang"`. Confirmed present on unmodified `main` before any of these changes. Out of scope for Phase 1 (not a schema/H1/title issue) — flagging for a future cleanup pass.
- `src/data/AqlTool.astro`, `src/data/FobCalculator.astro`, `src/data/Glossary.astro`, `src/data/LeadTimeEstimator.astro`, and `src/data/SETUP_INSTRUCTIONS.md` are also leftover duplicates of the `src/components/` versions, but since `src/data/` is not in `src/pages/`, Astro does not build them as routes — no live SEO impact, so left untouched to keep this change minimal. Could be deleted in a future repo-hygiene pass.

## Phase 2 — High priority
- [x] Keyword optimization (item 4) — **114 pages edited** with a natural "clothing manufacturer" (or close variant) mention added to the opening paragraph/H1 area, applying the Global vs Bangladesh rule throughout (verified: zero new "Bangladesh" mentions introduced across all 114 edits — checked via diff).
  - **Reconciling the count**: An initial automated scan (checking title + H1 + first ~100 words for the exact phrases "clothing/apparel/garment manufacturer") found 134 pages missing the keyword. Partway through, I judged that pages already containing "Clothing Manufacturing" (e.g. as a noun-phrase variant, same root word) — such as `insights/oem-vs-odm-vs-cmt.astro` or `manufacturing-costs.astro` — already satisfied the spirit of the requirement, so I widened acceptance to include "manufacturing" word-forms too and re-scanned. That re-scan found 35 pages genuinely still missing any variant; combined with the pages already edited before the criteria widened, **114 total files were edited**. Two pages (`booking.astro`, `visa-docs.astro`) do contain a natural "clothing manufacturer" mention in their visible body copy, but it falls slightly outside a strict first-~100-word window because these are transactional utility pages (factory-visit booking form, visitor document upload) with real content most of it JS-template boilerplate; forcing the phrase into their H1 ("Schedule a Factory Visit", "Visitor Document Submission") would be unnatural, so I left these two as-is rather than stuff the keyword — the mention already present is enough for real user- and crawler-facing relevance.
  - Every edit was hand-written per page based on that page's actual existing sentence (not a templated string) — verified by direct file diff, not a mechanical find/replace of one generic phrase.
  - Manual re-read pass done across all 114 edits for natural phrasing (see batch commits `13e5f86`, `51dbf11`, `b95bfda`, `e9970a3`, `132bec4` on branch `seo-fix-2026-07-part2`).
  - Homepage (`src/pages/index.astro`) was **not modified** for this item.
- [x] Tool supporting content (item 5) — see commit `b6594f5`: static "How to use this tool" + FAQ + FAQPage schema added to `/tools/lead-time-calculator`, `/tools/moq-calculator`, `/tools/sample-request`.
- [x] Missing schema (item 6) — see commit `48cb849`: 58 pages given appropriate JSON-LD; also fixed 41 pre-existing broken raw JSON-LD script tags discovered along the way (see note below).

## Additional finding (Phase 2)
While auditing schema, found `faq.astro` and 40 other pages had a **second, completely broken JSON-LD script tag** — raw unresolved Astro/JS expressions (`Astro.url.href`, `pageTitle`, `{allFaqs.map(...)}`) rendering as literal invalid text in production HTML (would fail Google Rich Results / any JSON-LD validator). This was pre-existing, not introduced by this work. Fixed all 41 by rebuilding with the correct `set:html={JSON.stringify(...)}` Astro pattern. Verified via full JSON-parse of all 301 built pages: 0 broken JSON-LD site-wide after the fix (was 41 broken before).
